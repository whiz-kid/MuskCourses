<div class="row" id="signin">
    <div class="col-md-4 col-md-offset-4">
        <h1 class="text-center page-header" style="color:crimson">Sign In</h1>
        {{# if hasErrors}}
            <div class="alert alert-danger">
                {{# each messages}}
                    <p>{{this}}</p>
                {{/each}}
            </div>
        {{/if}}
        <form action="/user/signin" method="post">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" class="form-control">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" class="form-control">
            </div>
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <button class="btn btn-success col-md-4 col-md-offset-4">Sign In</button>&nbsp;
             <h3>If not a member, Register<a href="/user/signup"> here</a></h3>
            <h3 class="text-center">or</h3>
            <a class="btn btn-block btn-social btn-facebook" href="/auth/facebook " style="margin-top:20px;">
                 <span class="fa fa-facebook"></span> Sign in with Facebook
            </a>

        </form>
    </div>
</div>